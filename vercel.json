{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm run build -- --no-lint"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
